<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>フォーム一覧</title>
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }
        th, td {
            border: 1px solid #ccc;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        a {
            color: blue;
        }
    </style>
</head>
<body>
    <h1>登録済みフォーム情報</h1>
    <table>
        <tr>
            <th>企業名</th>
            <th>URL（トップ）</th>
            <th>URL（問い合わせ）</th>
            <th>住所</th>
            <th>電話番号</th>
            <th>カテゴリ</th>
            <th>説明</th>
        </tr>
        {% for form in forms %}
        <tr>
            <td>{{ form.company_name }}</td>
            <td><a href="{{ form.url_top }}" target="_blank">トップ</a></td>
            <td><a href="{{ form.url_form }}" target="_blank">フォーム</a></td>
            <td>{{ form.address }}</td>
            <td>{{ form.tel }}</td>
            <td>{{ form.category_keywords }}</td>
            <td>{{ form.description }}</td>
        </tr>
        {% endfor %}
    </table>
</body>
</html>
