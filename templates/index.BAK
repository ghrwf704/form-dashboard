<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>フォーム一覧</title>
    <style>
        body { font-family: sans-serif; margin: 2em; }
        table { border-collapse: collapse; width: 100%; }
        th, td { padding: 8px; border: 1px solid #ccc; }
        th { background-color: #f2f2f2; }
        nav a { margin-right: 1em; }
    </style>
</head>
<body>

<nav>
    <a href="{{ url_for('index') }}">ダッシュボード</a>
    <a href="{{ url_for('manage_keywords') }}">キーワード管理</a>
    <a href="{{ url_for('logout') }}">ログアウト</a>
</nav>
<div class="weather-box">
  <p>📍 地域: {{ weather.city }}</p>
  <p>🌤 天気: {{ weather.description }}</p>
  <p>🌡 気温: {{ weather.temp }}℃</p>
  <p>💧 湿度: {{ weather.humidity }}%</p>
</div>
<p>こんにちは、{{ current_user.id }} さん！</p>
<p><strong>Bing検索時のキーワード:「 {{ active_keywords | join(' ') }} 」</strong></p>

<h1>企業一覧</h1>

<table>
    <tr>
        <th>企業名</th>
        <th>URL（トップ）</th>
        <th>URL（問い合わせ）</th>
        <th>住所</th>
        <th>電話番号</th>
        <th>FAX</th>
        <th>カテゴリ</th>
        <th>説明</th>
    </tr>
    {% for f in forms %}
    <tr>
        <td>{{ f.get("company_name", "") }}</td>
        <td><a href="{{ f.get('url_top', '#') }}" target="_blank">トップ</a></td>
        <td><a href="{{ f.get('url_form', '#') }}" target="_blank">フォーム</a></td>
        <td>{{ f.get("address", "") }}</td>
        <td>{{ f.get("tel", "") }}</td>
        <td>{{ f.get("fax", "") }}</td>
        <td>{{ f.get("category_keywords", "") }}</td>
        <td>{{ f.get("description", "") }}</td>
    </tr>
    {% endfor %}
</table>

</body>
</html>
